import 'package:flutter/material.dart';

class WorkoutWidget extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Workout widget"),
    );
  }


}